#include "shell.h"

/**
 * logicalOr - function that performs a logical OR operation on two boolean values.
 * @a: first boolean value.
 * @b: second boolean value.
 * Return: Result of the logical Or operation.
 */

bool logicalOr(bool a, bool b)
{
  return (a || b);
}
