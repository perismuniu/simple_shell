#include "shell.h"

/**
 * logicalAnd - function that performs a logical AND operation on two boolean values.
 * @a: first boolean value.
 * @b: second boolean value.
 * Return: Return of the logical AND operation.
 */

bool logicalAnd(bool a, bool b)
{
  return (a && b);
}
